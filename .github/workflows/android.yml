name: Debug TontiDette

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - run: tar -xzf tontidette.tar.gz
    
    - name: VOIR TOUT LE CONTENU
      run: |
        echo "=== RACINE ==="
        ls -la
        echo "=== DOSSIERS ==="
        ls -la */
        echo "=== CHERCHER GRADLEW ==="
        find . -name gradlew
